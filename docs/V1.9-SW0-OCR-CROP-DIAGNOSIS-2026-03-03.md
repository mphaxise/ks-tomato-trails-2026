# V1.9 SW-0 OCR Crop Diagnosis (2026-03-03)

## Scope
- Run date audited: `2026-03-03`
- Baseline experiment output:
  - `data/research/v1_6/ocr_recovery_2026-03-03/`
- Crop-fix experiment output:
  - `data/research/v1_6/ocr_recovery_2026-03-03_cropfix/`
- Live hard-row reviewer queue audited:
  - `tracker/hard-row-reviewer.html` (`58` rows from `2026-02-28` and `2026-03-01`)

## SW-0 Result (Root Cause Classification)
Primary failure is **crop targeting** (label crop band too low), with a secondary structural issue (multi-pot ambiguity in a single crop).

## Evidence
1. Baseline metrics (before crop fix)
- `focus_rows=32`
- `ensemble_match_rows=2` (`6.25%`)
- `manual_queue_rows=30`
- Best variant: `center_clahe_psm11` at `3.12%` pot-match rate

2. Crop-fix metrics (after moving label crop to upper/mid band and adding `label_adaptive_psm11`)
- `focus_rows=32`
- `ensemble_match_rows=9` (`28.12%`)
- `manual_queue_rows=23`
- Best variant: `label_adaptive_psm11` at `21.88%` pot-match rate

3. Lift from crop targeting change
- Ensemble match lift: `+7` rows (`2 -> 9`)
- Ensemble match rate lift: `+21.87` percentage points (`6.25% -> 28.12%`)
- Manual queue reduction: `-7` rows (`30 -> 23`, `-23.3%`)

4. Visual confirmation of mis-targeting in baseline label crop
- Example baseline label crops show soil/lower pot area while full image contains stakes:
  - `.../ocr_recovery_2026-03-03/manual_label_queue/2026-03-03_1T_192_AF1QipMc_label.jpg`
  - `.../ocr_recovery_2026-03-03/manual_label_queue/2026-03-03_1T_192_AF1QipMc_full.jpg`
  - `.../ocr_recovery_2026-03-03/manual_label_queue/2026-03-03_3T_194_AF1QipOu_label.jpg`

5. Remaining issue after crop fix
- OCR now hits more digits, but many are still from adjacent stakes in multi-pot frames.
- False/ambiguous reads remain high; crop fix improves signal but does not solve pot-specific disambiguation.

## SW-0b Reviewer Signal Audit (Live 58-row queue)
Derived from the embedded `rows` dataset in `tracker/hard-row-reviewer.html`:

- Total rows: `58`
- `matched_variant_count = 0`: `58/58` (`100%`)
- Rows with OCR numbers but none match suggested pot: `57/58`
- Rows with no OCR numbers: `1/58`

Interpretation:
- The reviewer queue is currently **100% placeholder suggestions** (no matched OCR evidence).
- Even when OCR detects numbers, they are not supporting the suggested pot IDs.

## Decision Gate for Sprint 1
Proceed to SW-1 only after:
1. Crop fix is merged into the main OCR recovery path used to generate reviewer queue.
2. Reviewer UI flags no-evidence rows explicitly (placeholder state), so human review time is not spent as if suggestions were evidence-backed.

## Immediate Next Actions
1. Apply crop-band fix in the production queue generator path (not only experiment run).
2. Add reviewer signal-tier UX:
   - `OCR-backed match`
   - `OCR numbers found, no pot match`
   - `No OCR signal`
3. Add explicit reviewer verdict: `No basis - cannot verify from this photo`.
4. Rebuild queue and re-measure distribution before assigning reviewer effort.
